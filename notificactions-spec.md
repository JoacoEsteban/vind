# Notifications settings Specification

The extension lacks a way to disable notifications (eg: binding activated)

The requirements for this feature are:

- New tab in settings for 'Notifications'
- Toggle controls for notifications (let's just start with binding activated)
- Persistance of key-boolean notifications in a new table with proper defaults
- Conditioning notification trigger with the new settings
